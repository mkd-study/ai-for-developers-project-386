{
  "name": "ai-for-developers-project-386",
  "version": "0.1.0",
  "private": true,
  "description": "Calendly-like booking service monorepo (TypeSpec contract + React frontend + Rust backend)",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "contract:compile": "pnpm --filter contract compile",
    "contract:watch": "pnpm --filter contract watch",
    "frontend:dev": "pnpm --filter frontend dev",
    "frontend:build": "pnpm --filter frontend build",
    "frontend:preview": "pnpm --filter frontend preview"
  }
}
